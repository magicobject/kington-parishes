User-agent: *
Allow: /

Sitemap: https://www.kingtonparishes.org.uk/sitemap.xml

# Live since 6 September 2026 — real pages are indexable, and only 404 and
# updates.html (the internal build changelog) carry <meta name="robots"
# content="noindex, nofollow">. sitemap.xml (scripts/build.mjs, regenerated
# on every build) already excludes both of those, via the same
# isSearchablePage check search indexing uses — see README.md's "Search
# visibility" section.
