# Backlog

Requests that came in while Claude was mid-task get parked here instead of
bloating that task's commit. Picked up automatically at the end of a
session that touches this repo — each item becomes its own commit, moved
to Done below once it lands (with the commit hash and build tag).

## Todo

## Done
